import { BrowserRouter, Routes, Route } from "react-router-dom";

function Routing() {
    return (
        <BrowserRouter>
            <Routes></Routes>
        </BrowserRouter>
    );
}
export default Routing;
